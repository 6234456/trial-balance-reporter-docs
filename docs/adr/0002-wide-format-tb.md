# ADR 0002 — Wide-format TB with Date Columns

## Status

Accepted.

## Decision

The `TB` sheet uses wide format. Metadata columns start with `#`; date amount columns are ISO dates.

## Consequences

This decision is binding for the MVP implementation and should be changed only through a later ADR.
