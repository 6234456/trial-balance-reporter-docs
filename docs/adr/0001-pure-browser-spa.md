# ADR 0001 — Pure Browser SPA

## Status

Accepted.

## Decision

The application runs entirely in the browser at runtime. It has no backend, no API server, no database, and no upload endpoint.

## Consequences

This decision is binding for the MVP implementation and should be changed only through a later ADR.
